#include "mateusz.h"
#include "Filip.h"
#include "marcin.h"
#include <stdio.h>
#include "gn.h"

int main(int arc,char * argv[]){

CopyDuzy(argv[1] , argv[2], 0);
/*
  char * source=argv[1];
  char * destiny=argv[2];
  toSleep(10);
  printf("%d\n",tryDelete(source,destiny));
        if (stat("xd.txt", &results) == 0)
            printf("File size: %d\n", results.st_size);
        else
            printf("Blad.\n");
        return 0;
*/
return 0;
}
